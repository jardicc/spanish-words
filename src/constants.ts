/** Minimum success rate (correct / total) for a word to be considered mastered. */
export const MASTERY_THRESHOLD = 0.8;

/** Minimum number of attempts before a word can be considered mastered. */
export const MASTERY_MIN_ATTEMPTS = 3;
